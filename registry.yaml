journeys:
  - journey: meter-reads
    params: [eref]
    actions:
      - action: email-click
        params: [utm]
      - action: form-view
        params: [source]
      - action: successful-submission
      - action: failed-submission
        params: [reason, source]
      - action: industry-accepted
      - action: industry-rejected
        params: [reason]
